import React from "react";

const SuspensePage = () => {
  return <p className="flex justify-center align-middle pt-48 text-xxl font-bolder">Loading ...</p>;
};
export default React.memo(SuspensePage);
